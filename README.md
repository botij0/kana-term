# kana-term

Terminal gojūon trainer for hiragana and katakana.

```bash
cargo run
cargo run -- hiragana
cargo run -- katakana
```

Needs a terminal font with Japanese glyphs (for example Noto Sans CJK).
